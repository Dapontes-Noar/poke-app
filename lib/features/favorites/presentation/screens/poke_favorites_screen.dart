import 'package:flutter/material.dart';

class PokeFavoritesScreen extends StatelessWidget {
  const PokeFavoritesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
