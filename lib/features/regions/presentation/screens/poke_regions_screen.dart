import 'package:flutter/material.dart';

class PokeRegionsScreen extends StatelessWidget {
  const PokeRegionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Regions Screen'),
      ),
    );
  }
}
