import 'package:flutter/material.dart';

class PokeProfileScreen extends StatelessWidget {
  const PokeProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Profile Screen'),
      ),
    );
  }
}
